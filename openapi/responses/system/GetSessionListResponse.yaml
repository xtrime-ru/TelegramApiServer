description: Response for session list
allOf:
- $ref: '../common/ApiResponse.yaml'
properties:
  success:
    type: boolean
    default: true
  response:
    $ref: '../../models/system/SessionList.yaml'